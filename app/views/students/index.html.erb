<section class="ToucanTech Students">
	<header>
		<h1>Students</h1>
	</header>
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Name</th>
				<th>Email</th>
				<th>Schools</th>
			</tr>
		</thead>
		<tbody>
			<%if @students.any? %>
        <% @students.each do |student| %>
          <tr>
            <td><%= student.name %></td>
            <td><%= student.email %></td>
            <td>
              <%student.schools.each do |school| %>
                <%= school.name%>
              <% end %>
            </td>
          </tr>
        <% end %>
      <% else %>
        <h1>No students to show</h1>
      <% end %>
		</tbody>
	</table>
	<%= link_to 'Add a student', new_student_path %>
</section>