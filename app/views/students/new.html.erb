<%= form_for @student do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :email %>
	<%= f.text_field :email %>
	<%= f.label :school %>
	<%= f.collection_select(:school_ids, School.all, :id, :name) %>
	<%= f.submit %>
<% end %> 